# Hermes runtime state and personal data
database/
data/
*.duckdb
*.duckdb.wal
*.wal
*_kuzu/
*.pid
*.log
argos_plugin/_mh_analysis.txt
hybrid_memory_service.json
hybrid_memory_service.starting
hybrid_memory.auto_extract.paused

# Python artifacts
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/

# Diagnostic chain-unfold PROBE files — carry personally-identifying example
# content (names, medication) for the recall investigation. NEVER commit
# these to a public repo; the analysis is done. The SANITIZED repro harness
# (eval_chain_unfold.py) IS committed and safe.
argos_plugin/eval/probe_*.py
argos_plugin/eval/eval_chain_unfold_priv.py

# Self-corpus eval outputs — carry personal memory content; never commit.
eval_self_corpus_out.jsonl
*_self_corpus_out.jsonl
*.out.jsonl

# Self-corpus regression gate — snapshots, gold sets, and gate scores carry
# personal memory content; never commit. Only the gold freeze sha (recorded
# in eval/gold/README.md) is committed.
argos_plugin/eval/snapshots/
eval/snapshots/
argos_plugin/eval/gold/*
!argos_plugin/eval/gold/README.md
eval/gold/*
!eval/gold/README.md
# Personal-domain answerer bench outputs — personal content; never commit.
argos_plugin/eval/bench/
eval/bench/
gate_scores.json
gate_baseline.json

# Node/Electron artifacts
node_modules/
dist/
build/

# Local configuration and credentials
.env
.env.*
!.env.example
config.local.yaml

# Editor and OS artifacts
.vscode/
.idea/
.DS_Store
Thumbs.db

# Devin-only build recipe (kept local; not part of the public release)
.devin/
