# Hermes runtime state and personal data
database/
data/
*.duckdb
*.duckdb.wal
*.wal
*_kuzu/
*.pid
*.log
hybrid_memory_plugin/_mh_analysis.txt
hybrid_memory_service.json
hybrid_memory_service.starting
hybrid_memory.auto_extract.paused

# Python artifacts
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/

# Diagnostic chain-unfold eval/probe files — carry personally-identifying
# example content (names, medication) for the recall investigation.
# NEVER commit these to a public repo; the analysis is done.
hybrid_memory_plugin/eval/eval_chain_unfold*.py
hybrid_memory_plugin/eval/probe_chain_miss.py
hybrid_memory_plugin/eval/probe_cosines.py
hybrid_memory_plugin/eval/probe_deep_search.py
hybrid_memory_plugin/eval/probe_fp.py

# Self-corpus eval outputs — carry personal memory content; never commit.
eval_self_corpus_out.jsonl
*_self_corpus_out.jsonl

# Node/Electron artifacts
node_modules/
dist/
build/

# Local configuration and credentials
.env
.env.*
!.env.example
config.local.yaml

# Editor and OS artifacts
.vscode/
.idea/
.DS_Store
Thumbs.db

# Devin-only build recipe (kept local; not part of the public release)
.devin/
