# Argos Issue Consolidation

Consolidated issue board for the Argos hybrid memory plugin.

**Board URL:** https://github.com/users/bobaba76/projects/3

## Current state

Updated 2026-08-30 (refreshed to match GitHub exactly).

- **31 open issues** / **44 closed** (75 total)
- Batches 1–5 and the security/db-pushdown/extractor/init-lifecycle clusters are fully closed
- Open work is batched: batch-6 (long-horizon), batch-7 (security round 2), batch-8 (retrieval), batch-9 (embedder), batch-10 (store integrity), batch-11 (graph hygiene), batch-12 (Cells-window specs, parked), batch-13 (low-severity sweep), batch-14 (eval track)

## Open issues (31)

### batch-6 (4 issues) — long-horizon + standalone

| # | Title | Notes |
|---|---|---|
| 10 | Stale-review sweep is dead config: parsed, documented, never scheduled | Smallest — engine exists (`review_pending.py`), needs a scheduler |
| 5 | Follow-up: upstream inline reminder queue after soak | Follow-up after #3 shipped |
| 15 | Extractor brittleness: English-first regexes, primitive sentence splitting, hardcoded lexicons | Biggest product-quality item |
| 6 | Memory lifecycle — archival tier, forgetting, long-horizon rollups (P5.1) | Largest feature; draft spec exists |

### batch-7 (5 issues) — security hardening round 2

| # | Title | Notes |
|---|---|---|
| 75 | Inbound scanner normalization order lets HTML-entity zero-width chars bypass the scan | Reorder unescape before zero-width strip |
| 85 | Extractor `_extract_facts_llm` egress import/gate outside try — can crash the turn | Fail-soft + top-level guard |
| 86 | Reviewer egress gate import/call unguarded — crash instead of pending_user_confirmation | Fail to human, not crash |
| 89 | Extractor regex ReDoS risk — no watchdog, no top-level guard on extract_from_turn | Shares the #85 guard |
| 87 | Memory service dispatch trusts client-supplied user_id — tenant spoofing | Defense-in-depth; matters for Cells |

### batch-8 (3 issues) — retrieval

| # | Title | Notes |
|---|---|---|
| 81 | Alias/traversal/PPR boost floors applied after the inclusion gate — below-gate never lifted | Boost before gate |
| 82 | `_content_exists` substring dedup silently drops distinct facts; LIMIT-500 scan unordered | Drop/gate substring layer |
| 84 | `_search_memories` graph block swallows all exceptions — 21/8 outage class invisible | Re-raise program errors |

### batch-9 (2 issues) — embedder resilience + hermetic tests

| # | Title | Notes |
|---|---|---|
| 83 | Embedder first load failure is permanent — NULL embeddings never scored | Retry + re-embed; root cause of #90 |
| 90 | Flaky `test_alias_expansion_injects_with_similarity_gate` — shared model cache | Test isolation (depends on #83) |

### batch-10 (5 issues) — store write-path integrity

| # | Title | Notes |
|---|---|---|
| 77 | `delete_memory` head-promotion path not transactional — crash leaves two active versions | Wrap in transaction |
| 78 | `resolve_conflict` supersedes without valid_to IS NULL / user_scope guards | Mirror `review_candidate` |
| 79 | `resolve_conflict` keep_new marks approved with no memory when remember() dedupes | Mirror `review_candidate` deduplicated |
| 80 | Timestamp normalization — lexicographic VARCHAR across mixed ISO forms; date-only expires_at never expires | Normalize at boundary; follow-on of #28/#33 |
| 93 | `structural_guard=True` is dead code — never wired into any runtime caller (#42 follow-up) | Wire into memory_update/consolidate or remove; verify guard fires |

### batch-11 (2 issues) — graph connection hygiene

| # | Title | Notes |
|---|---|---|
| 76 | graph.py consumes Kùzu results outside `_shared_conn_lock` — concurrent corruption/hang | Hold lock through consumption |
| 88 | graph.index_memory flushes per write (connection reopen) — batch cost + uncaught flush failures | Persists despite #31; default flush=False for batch |

### batch-12 (6 issues, opened 2026-08-29, parked — Cells window) — spec family

| # | Title | Spec |
|---|---|---|
| 67 | Spec-05 implementation: doc-fact namespace + client scope | `feature-specs/spec-05-doc-fact-namespace.md` |
| 68 | Spec-05 §D4: doc-fact trust/freshness fields (parked — activates with watcher spec) | same |
| 69 | Spec-06 implementation: access scoping (per-user ACL) | `feature-specs/spec-06-access-scoping.md` |
| 70 | Pilot audit-seed skeleton (parked — activates with watcher spec) | — |
| 71 | Spec-07 implementation: the watcher — document catalog, extraction & freshness | `feature-specs/spec-07-watcher-catalog.md` (closes #68 + #70) |
| 72 | POPIA & deployment mode: provider abstraction (cloud-first pilot, local SKU v2) | — |

### batch-13 (1 issue) — low-severity sweep

| # | Title | Notes |
|---|---|---|
| 91 | Low-severity sweep — Deque undefined name, values_conflict unit, purge_tombstone, dedup budget, `_flag` on, silent config errors, dead locals | Grouped for one batch; one-line fixes |

### batch-14 (3 issues) — eval track (benchmark clone + repro docs)

| # | Title | Notes |
|---|---|---|
| 74 | Benchmark ingest must fire store-level versioning — dedup=False inserts leave chains empty | Slot-linking in flight (`fix/issue-44-resume-v2`, 3 commits unpushed) |
| 95 | Distill store hurts gpt-4o — mechanism is retrieval ranking displacement, not content loss | Design trade-off; explore gating/ranking + docs note |
| 94 | 2×2 answerer×distill matrix is stale — both arms predate Aug 29 fixes (#28/#33) | Caveat now; re-run matrix after #74/#95 settle |

## Closed issues (44, grouped)

| Group | Issues | How |
|---|---|---|
| batch-1 | #19, #28, #33, #34 | PRs #56–#58 |
| batch-2 | #35, #39, #40, #43 | trust-model cluster, `b67fc59` |
| batch-3 | #8, #21, #26, #37, #38, #44 | PRs #53–#55, #62, #63 + direct commits |
| batch-4 | #16, #36, #41, #42, #47, #48 | PRs #59–#61 |
| batch-5 | #7, #20, #22, #49 | PRs #64–#66 |
| bug | #45, #51 | #45 fixed; #51 test-gate deadlock — PR #73 (open) |
| security | #17, #18, #24, #25 (+#23 superseded) | `c2ae889` sprint1 + PRs |
| db-pushdown | #9, #11, #27, #31 | `c2ae889` sprint1 |
| extractor | #14, #32, #46 | `c2ae889` sprint1 |
| init-lifecycle | #29, #30 | `c2ae889` sprint1 |
| docs | #12, #13 | Docs scoped (claim tightened) |
| unlabeled | #3, #4 | Shipped (reminder queue, value-supersession) |

Notable verdicts: **#8** — MemConflict 16q slice: UOCS 0.188→0.438 but mechanism is answerer-side (see #74); **#44** — eval-harness resume/checkpointing shipped in the benchmark clone (`fix/issue-44-resume-v2`, 13 tests).

## Build order

1. **Merge PR #92** (refactor/god-file-split) — master needs the split before per-batch branches
2. **batch-13**: #91 (low-severity sweep — can interleave anytime)
3. **batch-9**: #83 (embedder retry) → #90 (test isolation)
4. **batch-10**: #77/#78/#79 (write-path guards, mirrors shipped review_candidate work) → #80 (timestamp normalization) → #93 (wire or remove structural_guard; verify it fires)
5. **batch-11**: #76/#88 (graph connection hygiene)
6. **batch-7**: #85/#86 (egress fail-closed) → #89 (ReDoS guard) → #75 (scanner) → #87 (tenant authz)
7. **batch-8**: #84 → #81 → #82 (retrieval)
8. **batch-6**: #10 (stale-review sweep) → #5 (reminder queue follow-up) → #15 (extractor) → #6 (memory lifecycle P5.1)
9. **batch-12 (Cells window)**: #67 → #69 → #71 (+ parked #68/#70 when the watcher activates) → #72 (POPIA)
10. **batch-14 (eval track, parallel anytime)**: #74 (slot-linking) → #95 (distill mechanism) → #94 (caveat immediately; re-run matrix after #74/#95)
